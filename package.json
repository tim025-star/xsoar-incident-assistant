{
  "name": "xsoar-incident-assistant",
  "version": "3.0.0",
  "private": false,
  "description": "A local Playwright assistant for preparing incident-response drafts in Cortex XSOAR.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "start": "node src/server.js",
    "check": "node --check src/domain.js && node --check src/workflow.js && node --check src/page-adapter.js && node --check src/browser-session.js && node --check src/config.js && node --check src/server.js && node --check public/app.js && tsc --noEmit",
    "verify:browser": "node scripts/verify-ui.mjs"
  },
  "dependencies": {
    "playwright-core": "1.62.1"
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "typescript": "5.9.3"
  }
}
