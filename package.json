{
  "name": "xsoar-incident-assistant",
  "version": "0.3.1",
  "private": false,
  "description": "A local Playwright assistant for preparing incident-response drafts in Cortex XSOAR.",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "build": "vite build",
    "test": "npm run build && node --test",
    "prestart": "npm run build",
    "start": "node src/server.js",
    "check": "node scripts/check-node-version.mjs && node --check src/domain.js && node --check src/workflow.js && node --check src/page-adapter.js && node --check src/browser-session.js && node --check src/config.js && node --check src/local-ai.js && node --check src/local-ai-installer.js && node --check src/rpc.js && node --check src/server.js && node --check scripts/install-local-ai.mjs && node --check scripts/package-windows.mjs && tsc --noEmit",
    "verify:browser": "npm run build && node scripts/verify-ui.mjs",
    "package:windows": "node scripts/package-windows.mjs"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "@orpc/client": "^1.15.0",
    "@orpc/server": "^1.15.0",
    "hono": "^4.13.7",
    "playwright-core": "1.63.0",
    "solid-js": "^1.9.15",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "26.5.1",
    "tailwindcss": "^4.3.3",
    "typescript": "7.0.2",
    "vite": "^8.3.0",
    "vite-plugin-solid": "^2.11.14"
  }
}
